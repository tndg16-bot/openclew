---
name: task-tracker
description: タスクリスト・進捗を表示・管理するスキル
metadata: {"clawdbot":{"emoji":"📝"}}
version: 1.0.0
author: user
tags: [task, productivity, tracker, progress]
---

# Task Tracker Skill

タスクリスト・進捗を表示・管理するスキルです。

## 機能概要

1. **タスクリスト表示** - 現在のタスク一覧を表示
2. **進捗表示** - 各タスクのステータスと進捗率を表示
3. **優先度別表示** - 高・中・低優先度で色分け表示
4. **フィルタ機能** - ステータス、優先度、アサインされた人でフィルタ
5. **進捗更新** - タスクの進捗を更新
6. **タスク完了** - タスクを完了マーク

## トリガー

### タスク一覧表示
- 「タスク一覧」
- 「タスク状況」
- 「進捗教えて」
- 「タスクチェック」

### タスク操作
- 「タスク完了 【タスクIDまたは名前】」
- 「進捗更新 【タスク】 XX%」
- 「優先度変更 【タスク】 高/中/低」
- 「タスク追加 【タスク名】」

## 実行手順

### Step 1: タスクのロード

```javascript
// タスクデータをロード
const tasks = await loadTasks();

// フィルタリング
const filtered = filterTasks(tasks, filters);
```

### Step 2: タスク一覧の表示

```javascript
function displayTaskList(tasks) {
  let output = `📝 タスク一覧\n`;
  output += `━━━━━━━━━━━━━━━━━━━━\n\n`;

  // 優先度別にソート
  const sorted = sortTasks(tasks);

  for (const task of sorted) {
    output += formatTask(task);
  }

  return output;
}
```

### Step 3: タスクフォーマット

```
🔴 高優先度
[進行中] タスク名 (65%)
  作成日: 02/08 | 期限: 02/10 | アサイン: @user

🟡 中優先度
[待機中] タスク名 (0%)
  作成日: 02/07 | 期限: 02/12 | アサイン: @user

🟢 低優先度
[完了済み] タスク名 (100%)
  作成日: 02/06 | 完了日: 02/08 | アサイン: @user
```

### Step 4: 進捗の更新

```javascript
async function updateProgress(taskId, progress) {
  const tasks = await loadTasks();
  const task = tasks.find(t => t.id === taskId);

  if (task) {
    task.progress = progress;

    // ステータスを自動更新
    if (progress >= 100) {
      task.status = 'completed';
      task.completedAt = new Date().toISOString();
    } else if (progress > 0 && progress < 100) {
      task.status = 'in_progress';
    }

    await saveTasks(tasks);
  }
}
```

## 出力フォーマット

### タスク一覧の表示

```
📝 タスク一覧

📊 サマリー
━━━━━━━━━━━━━━━━━━━━
全タスク: 15件 | 完了: 8件 | 進行中: 4件 | 待機: 3件

🔴 高優先度 (3件)
━━━━━━━━━━━━━━━━━━━━
[進行中] 〇〇機能の実装 (65%)
  作成日: 02/08 | 期限: 02/10 | アサイン: @user
  進捗: [████████░░░░░░░░░] 65%

[待機中] △△のバグ修正 (0%)
  作成日: 02/07 | 期限: 02/09 | アサイン: @user

[待機中] □□の実装 (0%)
  作成日: 02/07 | 期限: 02/11 | アサイン: @user

🟡 中優先度 (7件)
━━━━━━━━━━━━━━━━━━━━
[進行中] xxxxxxの実装 (30%)
  作成日: 02/05 | 期限: 02/15 | アサイン: @user
  進捗: [███░░░░░░░░░░░░░] 30%

[完了済み] yyyyyyの実装 (100%)
  作成日: 02/03 | 完了日: 02/07 | アサイン: @user

🟢 低優先度 (5件)
━━━━━━━━━━━━━━━━━━━━
[完了済み] zzzzzzの実装 (100%)
  作成日: 02/01 | 完了日: 02/05 | アサイン: @user
```

## 設定

`config.json` でカスタマイズ可能：

```json
{
  "display": {
    "showCompleted": true,
    "showProgress": true,
    "sortOrder": "priority",  // priority, dueDate, createdDate
    "progressBar": true
  },
  "filters": {
    "defaultStatus": "all",  // all, pending, in_progress, completed
    "defaultPriority": "all"  // all, high, medium, low
  },
  "colors": {
    "high": "🔴",
    "medium": "🟡",
    "low": "🟢",
    "completed": "✅"
  },
  "storage": {
    "type": "file",
    "path": "./tasks.json"
  }
}
```

## 使用可能なツール

- `file.read/write` - タスクデータの永続化
- `discord.sendMessage` - Discordに送信
- `channels.send` - 複数チャンネル対応

## トラブルシューティング

### タスクが表示されない場合

1. **データファイルの確認**: `tasks.json` が存在し、有効なJSONか確認
2. **権限の確認**: ファイルの読み書き権限を確認
3. **フィルタ設定の確認**: フィルタが適切に設定されているか確認

### 進捗が更新されない場合

1. **タスクIDの確認**: 正しいタスクIDが指定されているか確認
2. **権限の確認**: タスクを更新する権限を持っているか確認
3. **保存の確認**: 変更が正しく保存されているか確認

## ベストプラクティス

1. **定期的な更新**: タスクの進捗を定期的に更新し、最新状態を維持
2. **明確な優先度**: 優先度を明確にし、タスクの順序を決める
3. **期限の遵守**: タスクに期限を設定し、遅延を防ぐ
4. **完了の報告**: タスク完了時に報告を行い、次のアクションを明確にする

## 将来の拡張案

- [ ] タスクのサブタスク対応
- [ ] タスクの依存関係の可視化
- [ ] タスクテンプレート機能
- [ ] タスクのアーカイブ機能
- [ ] タスクのエクスポート・インポート
- [ ] 複数人チーム対応
