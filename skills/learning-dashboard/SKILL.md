---
name: learning-dashboard
description: 学習データを可視化するWebダッシュボードスキル
metadata: {"clawdbot":{"emoji":"📊"}}
version: 1.0.0
author: user
tags: [dashboard, visualization, learning, analytics, charts]
---

# Learning Data Dashboard Skill

OpenClaw AIエージェントの学習データを可視化するWebダッシュボードを提供するスキルです。

## 概要

このスキルは、以下の機能を提供します：

1. **パターン可視化** - 学習された行動パターンをグラフで表示
2. **学習ログ閲覧** - 学習履歴をタイムライン形式で表示
3. **統計情報表示** - 信頼度、頻度などの統計データを表示
4. **リアルタイム更新** - WebSocketによるリアルタイムデータ更新
5. **データエクスポート** - JSON/CSV形式でデータをエクスポート

## 起動方法

### 方法1: コマンドラインから直接実行

```bash
node skills/learning-dashboard/server.js
```

### 方法2: 環境変数でポートを指定

```bash
DASHBOARD_PORT=8080 node skills/learning-dashboard/server.js
```

## Webサーバー

### エンドポイント

| パス | メソッド | 説明 |
|------|---------|------|
| `/` | GET | ダッシュボードHTML |
| `/api/patterns` | GET | パターン一覧取得 |
| `/api/learning-log` | GET | 学習ログ取得 |
| `/api/stats` | GET | 統計情報取得 |
| `/api/behavior-analysis` | GET | 行動分析データ取得 |
| `/api/realtime-data` | GET | リアルタイムデータ取得 |
| `/api/export-data` | GET | データエクスポート |

### クエリパラメータ

#### `/api/patterns`

| パラメータ | 型 | デフォルト | 説明 |
|-----------|----|-----------|------|
| `type` | string | all | パターンタイプフィルタ |
| `limit` | number | 100 | 取得するパターン数 |

#### `/api/learning-log`

| パラメータ | 型 | デフォルト | 説明 |
|-----------|----|-----------|------|
| `limit` | number | 50 | 取得するエントリ数 |
| `offset` | number | 0 | 開始位置 |

#### `/api/export-data`

| パラメータ | 型 | デフォルト | 説明 |
|-----------|----|-----------|------|
| `format` | string | json | エクスポート形式 (json/csv) |

## 設定

### config.json

```json
{
  "server": {
    "enabled": true,
    "port": 3000,
    "host": "localhost"
  },
  "refresh": {
    "interval": 30000,
    "autoRefresh": true
  },
  "data": {
    "patternsPath": "skills/self-learning-agent/data/patterns.json",
    "learningLogPath": "skills/self-learning-agent/data/learning-log.json",
    "exportPath": "exports/learning-data"
  },
  "visualization": {
    "chartJsVersion": "4.4.1",
    "theme": "dark",
    "maxPatterns": 10,
    "maxLogEntries": 50
  },
  "websocket": {
    "enabled": true,
    "heartbeatInterval": 60000
  },
  "export": {
    "formats": ["json", "csv"],
    "autoExport": false,
    "schedule": "daily"
  },
  "caching": {
    "enabled": true,
    "ttl": 300000
  }
}
```

## 可視化

### チャート

1. **Behavior Patterns Chart** - 時間帯別のアクティビティ頻度
2. **Day of Week Chart** - 曜日別のアクティビティ
3. **Confidence Distribution** - 信頼度分布

### 統計カード

- **Total Patterns** - 検出されたパターン総数
- **Active Patterns** - アクティブなパターン数
- **Avg Confidence** - 平均信頼度
- **Learning Entries** - 学習ログエントリ数
- **Uptime** - 稼働時間

## WebSocket

### 接続

```
ws://localhost:3000
```

### メッセージフォーマット

```json
{
  "type": "refresh",
  "data": {
    "timestamp": "2026-02-09T00:00:00Z"
  }
}
```

## データソース

ダッシュボードは以下のデータソースからデータを取得します：

1. **Self-Learning Agent** - パターン認識、行動分析
2. **Context Sharing Manager** - 学習ログ、コンテキストデータ

## 依存関係

```json
{
  "dependencies": {
    "http": "*",
    "fs": "*",
    "path": "*",
    "chart.js": "^4.4.1"
  }
}
```

## エラーハンドリング

| HTTPステータス | エラータイプ | 説明 |
|--------------|--------------|------|
| 200 | OK | リクエスト成功 |
| 404 | Not Found | リソースが存在しない |
| 500 | Internal Server Error | サーバーエラー |

## セキュリティ

1. **CORS**: デフォルトで全ドメインを許可（本番環境では制限推奨）
2. **データアクセス**: ローカルファイルのみ読み取り
3. **エクスポート**: データはローカルにのみ保存

## 拡張機能

### 追加予定

1. **ユーザー認証** - ログイン機能
2. **複数エージェント対応** - 複数のAIエージェントデータを表示
3. **高度な分析** - トレンド分析、予測機能
4. **カスタマイズ** - テーマ、チャートのカスタマイズ

---

**文書バージョン**: 1.0.0
**最終更新**: 2026-02-09
