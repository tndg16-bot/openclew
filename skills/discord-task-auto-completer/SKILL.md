---
name: discord-task-auto-completer
description: Discordのログを解析し、タスクを自動抽出・管理・完了するスキル
metadata: {"clawdbot":{"emoji":"✅"}}
version: 1.0.0
author: user
tags: [discord, task, automation, productivity, learning]
---

# Discord Task Auto-Completer Skill

Discordのログ・メッセージを解析し、タスクを自動抽出・管理・完了するAIアシスタントスキルです。

## コア機能

1. **タスク抽出** - Discordメッセージからタスクキーワードを自動検出
2. **優先度判定** - タスクの緊急度をAIで分析
3. **期限設定** - タスクの期限を推定・設定
4. **進捗追跡** - メッセージのやり取りから進捗を自動更新
5. **自動完了** - 完了の兆候（特定キーワード・反応）を検出して完了マーク
6. **学習・適応** - ユーザーのタスク管理スタイルを学習

## トリガー

### 自動実行
- スケジュール: 毎時0分、12分、30分、45分（15分おき）
- メッセージ受信時: リアルタイムで新規メッセージを解析

### 手動実行
- 「タスク一覧」
- 「タスク状況を教えて」
- 「今のタスクは？」
- 「完了したタスク」

## タスク検出パターン

### キーワードベースの検出
以下のキーワードを含むメッセージをタスクとして抽出：

```
日本語: やること, タスク, しないと, 実装, 作る, 修正, 対応, 準備, 調査, 確認
英語: do, task, todo, implement, create, fix, respond, prepare, investigate, check
```

### 状態遷移の自動検出

| キーワード | アクション | 新規ステータス |
|-----------|----------|--------------|
| 「作成」,「実装開始」 | 新規タスク作成 | `pending` |
| 「やった」,「完了」,「解決」 | タスク完了 | `completed` |
| 「着手」,「開始」 | 進捗更新 | `in_progress` |
| 「保留」,「後で」 | タスク一時停止 | `paused` |
| 「問題」,「バグ」 | 優先度上昇 | `high` |

## 実行手順

### Step 1: Discordログのスキャン

```javascript
// 過去N日間のメッセージを取得
const messages = await scanDiscordMessages({
  guildId: config.guildId,
  channelId: config.taskChannelId,
  days: config.analysis.lookbackDays
});

// 各メッセージからタスクを抽出
const tasks = messages.map(msg => extractTask(msg));
```

### Step 2: タスク構造の作成

```javascript
{
  "id": "task-001",
  "title": "〇〇機能の実装",
  "description": "メッセージの全文または要約",
  "priority": "high",  // high, medium, low
  "status": "pending",  // pending, in_progress, completed, paused
  "assignedTo": "@user",
  "createdAt": "2026-02-09T10:00:00Z",
  "estimatedDeadline": "2026-02-10T18:00:00Z",
  "deadline": null,  // 明示的な期限
  "progress": 0,  // 0-100%
  "tags": ["implementation", "priority"],
  "relatedMessages": ["msg-id-1", "msg-id-2"]
}
```

### Step 3: 進捗の自動更新

メッセージの流れを分析して、タスクの進捗を推定：

```javascript
function estimateProgress(task, newMessage) {
  // メッセージ数の増加 → アクティビティ
  task.activityScore += 1;

  // 完了の兆候キーワード
  const completionKeywords = ['完了', 'やった', '解決', 'finished', 'done'];
  if (completionKeywords.some(kw => newMessage.includes(kw))) {
    task.progress = 100;
    task.status = 'completed';
    task.completedAt = new Date().toISOString();
  }

  return task;
}
```

### Step 4: 自動完了の判定

以下の条件のいずれかを満たす場合、タスクを自動完了にする：

1. **完了キーワード検出**: メッセージに完了を示すキーワードを含む
2. **最後の反応**: 最後のメッセージから24時間以上経過
3. **承認反応**: 特定の絵文字（✅, 🎉, 🙌）が含まれる
4. **明示的マーク**: ユーザーが「完了マーク」と明示的に言及

```javascript
function shouldAutoComplete(task, messages) {
  const lastMessage = messages[messages.length - 1];

  // 完了キーワードチェック
  if (containsCompletionKeyword(lastMessage.content)) {
    return { reason: 'keyword', confidence: 0.9 };
  }

  // 絵文字チェック
  if (lastMessage.content.includes('✅') || lastMessage.content.includes('🙌')) {
    return { reason: 'emoji', confidence: 0.85 };
  }

  // 時間経過チェック（最後のメッセージから24時間）
  if (task.lastActivity && isOlderThan(task.lastActivity, '24h')) {
    return { reason: 'inactivity', confidence: 0.5 };
  }

  return { reason: null, confidence: 0 };
}
```

### Step 5: レポート生成

```
✅ タスク自動管理レポート

📊 現在のタスク状況
━━━━━━━━━━━━━━━━━━━━━━
全タスク: 15件
✅ 完了済み: 8件
🔄 進行中: 4件
⏳ 待機中: 3件

🔥 高優先度タスク (3件)
━━━━━━━━━━━━━━━━━━━━━━
1. 〇〇機能の実装 [進行中]
   優先度: 高 | 作成日: 02/08
   進捗: 60% | 最終アクティビティ: 4時間前

2. △△のバグ修正 [待機中]
   優先度: 高 | 作成日: 02/07
   期限推定: 今日中

3. □□の実装 [進行中]
   優先度: 高 | 作成日: 02/08
   進捗: 30% | 最終アクティビティ: 2時間前

💬 今のアクティビティ
━━━━━━━━━━━━━━━━━━━━━━
最近30分間: 5件のメッセージ
最後のタスク更新: 15分前
```

## 設定

`config.json` でカスタマイズ可能：

```json
{
  "discord": {
    "guildId": "your_guild_id",
    "taskChannelId": "your_channel_id",
    "trackChannels": ["tasks", "general"],
    "ignoreBots": true
  },
  "analysis": {
    "lookbackDays": 7,
    "taskKeywords": ["やる", "タスク", "実装", "作る", "修正"],
    "completionKeywords": ["完了", "やった", "解決", "finished", "✅"],
    "priorityKeywords": {
      "high": ["緊急", "重要", "優先", "urgent"],
      "medium": ["通常", "中", "normal"],
      "low": ["低い", "小", "low"]
    }
  },
  "autoComplete": {
    "enabled": true,
    "inactivityThreshold": "24h",
    "confidenceThreshold": 0.7,
    "requireConfirmation": false
  },
  "learning": {
    "enabled": true,
    "adaptKeywords": true,
    "learnUserPatterns": true,
    "feedbackCollection": true
  },
  "notifications": {
    "channels": ["discord"],
    "dailyReport": {
      "enabled": true,
      "time": "21:00"
    },
    "realtime": {
      "enabled": true,
      "onTaskCreated": true,
      "onTaskCompleted": true
    }
  },
  "storage": {
    "type": "file",
    "path": "./tasks.json",
    "backupEnabled": true,
    "backupInterval": "1h"
  }
}
```

## 自己学習機能

### キーワード適応

ユーザーの使い方に応じて、検出キーワードを自動追加：

```javascript
// 新しいキーワードを学習
if (userExplicitlyDefinesKeyword("これはタスクです")) {
  learning.addKeyword("タスクです", "task");
}

// 検出精度向上
const customKeywords = learning.getCustomKeywords();
const detected = extractTask(message, customKeywords);
```

### パターン学習

ユーザーのタスク管理パターンを分析：

```javascript
// 例: ユーザーが夕方にタスク完了傾向がある
if (analysis.userCompletionsPeakTime === "evening") {
  notifications.schedule("completion_reminder", "17:00");
}

// 例: ユーザーが特定の絵文字で完了を示す傾向がある
if (analysis.userPreferredCompletionEmoji === "🎉") {
  autoComplete.emojiThreshold = 0.95; // 🎉が含まれたら確実に完了
}
```

## 使用可能なツール

- `discord.getMessages` - 過去のメッセージ取得
- `discord.sendMessage` - レポート送信
- `file.read/write` - タスクデータの永続化
- `memory.search` - 過去のタスク参照

## トラブルシューティング

### タスクが検出されない場合

1. **キーワード設定を確認**: `config.json` の `taskKeywords`
2. **チャンネル設定を確認**: 正しいチャンネルIDが設定されているか
3. **パーミッション確認**: Botがメッセージ履歴を読める権限を持っているか

### 自動完了が間違っている場合

1. **信頼度閾値を調整**: `autoComplete.confidenceThreshold`
2. **確認を有効化**: `autoComplete.requireConfirmation: true`
3. **手動で修正**: 「完了取り消し」等のキーワードで修正可能

### リソース使用量が多い場合

1. **検索範囲を制限**: `analysis.lookbackDays` を減らす
2. **追跡チャンネルを制限**: `discord.trackChannels` を最小化
3. **通知頻度を減らす**: `notifications.realtime.enabled` を `false` に

## ベストプラクティス

1. **タスクチャンネルの分離**: タスク管理専用チャンネルを作成
2. **明確なフォーマット**: メッセージにタスクIDや期限を含める
3. **定期的な振り返り**: 毎日のレポートで状況を確認
4. **学習機能の活用**: ユーザーのパターンを反映させる

## 将来の拡張案

- [ ] タスクのサブタスク対応
- [ ] 依存関係の自動検出（「〇〇が終わってから△△」）
- [ ] 複数人チームでのタスク割り当て
- [ ] 他ツールとの統合（GitHub Issues, JIRA, Trello）
- [ ] タスクテンプレート機能
